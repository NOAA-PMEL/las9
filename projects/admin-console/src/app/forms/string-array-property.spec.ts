import { StringArrayProperty } from './string-array-property';

describe('StringArrayProperty', () => {
  it('should create an instance', () => {
    expect(new StringArrayProperty()).toBeTruthy();
  });
});
